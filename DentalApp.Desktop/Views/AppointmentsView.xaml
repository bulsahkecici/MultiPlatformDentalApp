<UserControl x:Class="DentalApp.Desktop.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:DentalApp.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <Style x:Key="AvailableSlotStyle" TargetType="Button">
            <Setter Property="Background" Value="#90EE90"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#70C070"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7ACC7A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="OccupiedSlotStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="UnavailableSlotStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#B91C1C"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Randevu Yönetimi" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                   Foreground="{StaticResource BulkaDarkBlue}"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <DatePicker SelectedDate="{Binding SelectedDate}" 
                       materialDesign:HintAssist.Hint="Tarih Seç"
                       Language="tr-TR"
                       Margin="0,0,20,0"
                       Width="200"/>
            
            <Button materialDesign:ButtonAssist.CornerRadius="5" 
                    Command="{Binding RefreshCommand}" 
                    Margin="0,0,10,0"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Margin="0,0,5,0"/>
                    <TextBlock Text="YENİLE"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding AddAppointmentCommand}"
                    Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0"/>
                    <TextBlock Text="RANDEVU EKLE"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- SCHEDULER GRID -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Time column header -->
                <Border Grid.Row="0" Grid.Column="0" 
                        Background="{StaticResource BulkaMediumBlue}" 
                        Padding="10"
                        BorderBrush="{StaticResource BulkaDarkBlue}"
                        BorderThickness="1">
                    <TextBlock Text="Saat" 
                             Foreground="White"
                             FontWeight="Bold"
                             HorizontalAlignment="Center"/>
                </Border>
                
                <!-- Dentist headers -->
                <ItemsControl Grid.Row="0" Grid.Column="1" 
                            ItemsSource="{Binding Dentists}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding Dentists.Count}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BulkaMediumBlue}" 
                                  Padding="10"
                                  BorderBrush="{StaticResource BulkaDarkBlue}"
                                  BorderThickness="1,1,1,1">
                                <TextBlock Text="{Binding Name}" 
                                         Foreground="White"
                                         FontWeight="Bold"
                                         TextWrapping="Wrap"
                                         TextAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Time slots and appointment slots -->
                <ItemsControl Grid.Row="1" Grid.Column="0" 
                            ItemsSource="{Binding TimeSlots}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Height="40"
                                  BorderBrush="{StaticResource BulkaDarkBlue}"
                                  BorderThickness="0,0,1,1"
                                  Background="White"
                                  Padding="5">
                                <TextBlock Text="{Binding TimeString}" 
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"
                                         FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Appointment slots grid -->
                <ItemsControl Grid.Row="1" Grid.Column="1" 
                            ItemsSource="{Binding AppointmentSlots}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="{Binding DataContext.TimeSlots.Count, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       Columns="{Binding DataContext.Dentists.Count, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:AppointmentSlot}">
                            <Button Command="{Binding DataContext.SlotClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}"
                                  Height="40"
                                  Margin="0,0,1,1">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Padding" Value="5"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Style.Triggers>
                                            <!-- Status-based styling -->
                                            <DataTrigger Binding="{Binding Status}" Value="Available">
                                                <Setter Property="Background" Value="#90EE90"/>
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="#70C070"/>
                                                <Setter Property="IsEnabled" Value="True"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Occupied">
                                                <Setter Property="Background" Value="#3B82F6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                                <Setter Property="IsEnabled" Value="True"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Unavailable">
                                                <Setter Property="Background" Value="#DC2626"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="#B91C1C"/>
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Setter Property="Opacity" Value="0.6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding TimeString}" 
                                             FontSize="10"
                                             HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding PatientName}" 
                                             FontSize="9"
                                             TextWrapping="Wrap"
                                             HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PatientName, Converter={StaticResource NullToBooleanConverter}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     HorizontalAlignment="Center" VerticalAlignment="Center" Width="60" Height="60"/>
    </Grid>
</UserControl>