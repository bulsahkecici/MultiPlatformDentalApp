<UserControl x:Class="DentalApp.Desktop.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Randevu Yönetimi" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                   Foreground="{StaticResource BulkaDarkBlue}"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <DatePicker SelectedDate="{Binding SelectedDate}" 
                       materialDesign:HintAssist.Hint="Tarih Seç"
                       Language="tr-TR"
                       Margin="0,0,20,0"
                       Width="200"/>
            
            <Button materialDesign:ButtonAssist.CornerRadius="5" 
                    Command="{Binding RefreshCommand}" 
                    Margin="0,0,10,0"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Margin="0,0,5,0"/>
                    <TextBlock Text="YENİLE"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding AddAppointmentCommand}"
                    Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0"/>
                    <TextBlock Text="RANDEVU EKLE"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding EditAppointmentCommand}"
                    Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Edit" Margin="0,0,5,0"/>
                    <TextBlock Text="DÜZENLE"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CancelAppointmentCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Cancel" Margin="0,0,5,0"/>
                    <TextBlock Text="İPTAL ET"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <materialDesign:Card Grid.Row="2" VerticalAlignment="Stretch">
            <DataGrid ItemsSource="{Binding Appointments}" 
                      SelectedItem="{Binding SelectedAppointment}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tarih" Binding="{Binding AppointmentDate, StringFormat=d}" Width="Auto"/>
                    <DataGridTextColumn Header="Saat" Binding="{Binding StartTime, StringFormat=hh\\:mm}" Width="Auto"/>
                    <DataGridTextColumn Header="Hasta" Binding="{Binding PatientFullName}" Width="*"/>
                    <DataGridTextColumn Header="Tür" Binding="{Binding AppointmentType}" Width="*"/>
                    <DataGridTextColumn Header="Durum" Binding="{Binding Status, Converter={StaticResource StatusToTurkishConverter}}" Width="Auto"/>
                    <DataGridTextColumn Header="Diş Hekimi" Binding="{Binding DentistEmail}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Command="{Binding PreviousPageCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,10,0">
                <materialDesign:PackIcon Kind="ChevronLeft"/>
            </Button>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" Margin="10,0"/>
            <Button Command="{Binding NextPageCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="10,0,0,0">
                <materialDesign:PackIcon Kind="ChevronRight"/>
            </Button>
        </StackPanel>

        <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     HorizontalAlignment="Center" VerticalAlignment="Center" Width="60" Height="60"/>
    </Grid>
</UserControl>
