<UserControl x:Class="DentalApp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Width="400" Padding="40" UniformCornerRadius="10" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Dental App Login" Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Email" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Margin="0,0,0,20"/>
                
                <PasswordBox x:Name="PasswordBox" 
                             materialDesign:HintAssist.Hint="Password" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             Margin="0,0,0,10">
                    <!-- PasswordBinding helper would be better, but for simplicity let's use a workaround or code-behind -->
                </PasswordBox>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,5,0,10" />
                
                <Button Content="LOGIN" 
                        Command="{Binding LoginCommand}" 
                        IsDefault="True"
                        Margin="0,20,0,0"
                        Height="45"
                        materialDesign:ButtonProgressAssist.IsIndeterminate="{Binding IsBusy}"
                        materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
