<UserControl x:Class="DentalApp.Desktop.Views.DentistEarningsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Kazançlarım" 
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                       Foreground="{StaticResource BulkaDarkBlue}"
                       FontWeight="Bold"
                       Margin="0,0,20,0"/>
            <Button Command="{Binding RefreshCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    Foreground="{StaticResource BulkaMediumBlue}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Margin="0,0,5,0" Foreground="{StaticResource BulkaMediumBlue}"/>
                    <TextBlock Text="YENİLE" Foreground="{StaticResource BulkaMediumBlue}"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- 3 Kart: Ciro, Ödenen Ciro Payı, Toplam Kazanç -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <materialDesign:Card Margin="10" Padding="30" Background="White">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CurrencyUsd" 
                                           Width="56" 
                                           Height="56" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,15"
                                           Foreground="{StaticResource BulkaMediumBlue}"/>
                    <TextBlock Text="{Binding TotalTurnover, Converter={StaticResource DecimalToTurkishStringConverter}, ConverterParameter=' ₺'}" 
                             Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                             Foreground="{StaticResource BulkaDarkBlue}"
                             FontWeight="Bold"
                             FontSize="24"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             TextWrapping="Wrap"/>
                    <TextBlock Text="Ciro" 
                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                             Foreground="{StaticResource BulkaMediumBlue}"
                             HorizontalAlignment="Center"
                             Margin="0,10,0,0"/>
                    <TextBlock Text="Toplam yaptığı işlerin maliyeti" 
                             Style="{StaticResource MaterialDesignBody2TextBlock}" 
                             Foreground="Gray"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             TextWrapping="Wrap"
                             Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Margin="10" Padding="30" Background="White">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CashCheck" 
                                           Width="56" 
                                           Height="56" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,15"
                                           Foreground="{StaticResource BulkaLightGreen}"/>
                    <TextBlock Text="{Binding PaidTurnoverShare, Converter={StaticResource DecimalToTurkishStringConverter}, ConverterParameter=' ₺'}" 
                             Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                             Foreground="{StaticResource BulkaDarkBlue}"
                             FontWeight="Bold"
                             FontSize="24"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             TextWrapping="Wrap"/>
                    <TextBlock Text="Ödenen Ciro Payı" 
                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                             Foreground="{StaticResource BulkaMediumBlue}"
                             HorizontalAlignment="Center"
                             Margin="0,10,0,0"/>
                    <TextBlock Text="{Binding CommissionRate, StringFormat='Ciro Payı: {0:F1}%'}" 
                             Style="{StaticResource MaterialDesignBody2TextBlock}" 
                             Foreground="Gray"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Margin="10" Padding="30" Background="{StaticResource BulkaLightBlue}">
                <StackPanel>
                    <materialDesign:PackIcon Kind="TrendingUp" 
                                           Width="56" 
                                           Height="56" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,15"
                                           Foreground="{StaticResource BulkaDarkBlue}"/>
                    <TextBlock Text="{Binding TotalEarnings, Converter={StaticResource DecimalToTurkishStringConverter}, ConverterParameter=' ₺'}" 
                             Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                             Foreground="{StaticResource BulkaDarkBlue}"
                             FontWeight="Bold"
                             FontSize="24"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             TextWrapping="Wrap"/>
                    <TextBlock Text="Toplam Kazanç" 
                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                             Foreground="{StaticResource BulkaDarkBlue}"
                             HorizontalAlignment="Center"
                             Margin="0,10,0,0"/>
                    <TextBlock Text="{Binding Salary, StringFormat='Maaş: {0:N2} ₺'}" 
                             Style="{StaticResource MaterialDesignBody2TextBlock}" 
                             Foreground="{StaticResource BulkaDarkBlue}"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             Margin="0,5,0,0"/>
                    <TextBlock Text="Maaş + Ödenen Ciro Payı" 
                             Style="{StaticResource MaterialDesignBody2TextBlock}" 
                             Foreground="Gray"
                             HorizontalAlignment="Center"
                             TextAlignment="Center"
                             TextWrapping="Wrap"
                             Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>

        <materialDesign:Card Grid.Row="2" Margin="10" Padding="20" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Tamamlanan Tedaviler"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           Foreground="{StaticResource BulkaDarkBlue}"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Treatments}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          MaxHeight="300"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tarih" Binding="{Binding TreatmentDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="100"/>
                        <DataGridTextColumn Header="Hasta" Binding="{Binding PatientFullName}" Width="*"/>
                        <DataGridTextColumn Header="Tedavi" Binding="{Binding TreatmentType}" Width="*"/>
                        <DataGridTextColumn Header="Maliyet" Binding="{Binding Cost, Converter={StaticResource DecimalToTurkishStringConverter}, ConverterParameter=' ₺'}" Width="100"/>
                        <DataGridTextColumn Header="Kazanç" Binding="{Binding Earnings, Converter={StaticResource DecimalToTurkishStringConverter}, ConverterParameter=' ₺'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <ProgressBar Grid.Row="2" 
                     Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     Width="60" 
                     Height="60"/>
    </Grid>
</UserControl>