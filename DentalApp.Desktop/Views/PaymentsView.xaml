<UserControl x:Class="DentalApp.Desktop.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ödeme ve İndirim Yönetimi" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                   Foreground="{StaticResource BulkaDarkBlue}"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <materialDesign:Card Grid.Row="1" Padding="20" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sol Panel: Hasta ve Tedavi Planı Seçimi -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Hasta Seçimi" 
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                               Foreground="{StaticResource BulkaDarkBlue}"
                               Margin="0,0,0,10"/>

                    <ComboBox ItemsSource="{Binding Patients}"
                             SelectedItem="{Binding SelectedPatient}"
                             DisplayMemberPath="FullName"
                             materialDesign:HintAssist.Hint="Hasta Seçin"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="Tedavi Planı" 
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                               Foreground="{StaticResource BulkaDarkBlue}"
                               Margin="0,0,0,10"/>

                    <ComboBox ItemsSource="{Binding TreatmentPlans}"
                             SelectedItem="{Binding SelectedTreatmentPlan}"
                             DisplayMemberPath="Title"
                             materialDesign:HintAssist.Hint="Tedavi Planı Seçin"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="İndirim Uygula" 
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                               Foreground="{StaticResource BulkaDarkBlue}"
                               Margin="0,20,0,10"/>

                    <TextBox Text="{Binding DiscountAmount, UpdateSourceTrigger=PropertyChanged}" 
                             materialDesign:HintAssist.Hint="İndirim Tutarı (TRY)" 
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,10"/>

                    <TextBox Text="{Binding DiscountPercentage, UpdateSourceTrigger=PropertyChanged}" 
                             materialDesign:HintAssist.Hint="İndirim Yüzdesi (%)" 
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,0,20"/>

                    <Button Content="İndirim Uygula" 
                           Command="{Binding ApplyDiscountCommand}"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           Background="{StaticResource BulkaMediumBlue}"
                           Foreground="White"
                           Margin="0,0,0,10"/>

                    <Button Content="Ödeme İşle" 
                           Command="{Binding ProcessPaymentCommand}"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           Background="{StaticResource BulkaMediumBlue}"
                           Foreground="White"/>
                </StackPanel>

                <!-- Sağ Panel: Bilgiler -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Bilgiler" 
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                               Foreground="{StaticResource BulkaDarkBlue}"
                               Margin="0,0,0,20"/>

                    <materialDesign:Card Padding="15" Background="{StaticResource BulkaLightBlue}">
                        <StackPanel>
                            <TextBlock Text="Seçili Tedavi Planı:" 
                                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedTreatmentPlan.Title}" 
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       FontWeight="Bold"
                                       Margin="0,0,0,15"/>

                            <TextBlock Text="Toplam Tutar:" 
                                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedTreatmentPlan.TotalEstimatedCost, StringFormat='{}{0:F2} TRY'}" 
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
